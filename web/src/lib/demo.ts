/**
 * The one project the walkthrough, the sidebar fallback and the portfolio claim
 * all point at. It was three separate literals in three files, which is how a
 * demo ends up half-navigating to a project that no longer exists.
 *
 * PRJ-004217 — "the silent failure": spend keeps climbing while physical
 * progress flattens, and it carries a stored replay with a measured lead time.
 */
export const HERO_PROJECT = 'PRJ-004217';
